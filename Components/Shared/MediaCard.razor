@* Presentational poster card. Trailer/Watch buttons appear on hover. *@

<div class="group card overflow-hidden">
    <div class="relative aspect-[2/3] overflow-hidden bg-surface-2">
        @if (PosterUrl is not null)
        {
            <img src="@PosterUrl" alt="@Title poster" loading="lazy"
                 class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105" />
        }
        else
        {
            <div class="flex h-full items-center justify-center text-muted">No image</div>
        }

        <div class="absolute right-2 top-2">
            <RatingBadge Score="Score" />
        </div>

        @* Hover overlay: gradient + overview + action buttons. *@
        <div class="absolute inset-0 flex flex-col justify-end gap-3 bg-gradient-to-t from-ink via-ink/80 to-transparent p-3 opacity-0 transition-opacity duration-200 group-hover:opacity-100">
            @if (!string.IsNullOrWhiteSpace(Overview))
            {
                <p class="line-clamp-4 text-xs text-content/90">@Overview</p>
            }
            <div class="flex gap-2">
                <a href="@TrailerUrl" class="btn-ghost flex-1 px-2 py-1.5 text-xs">Trailer</a>
                <a href="@WatchUrl" class="btn-primary flex-1 px-2 py-1.5 text-xs">▶ Watch</a>
            </div>
        </div>
    </div>

    <div class="p-3">
        <h3 class="truncate text-sm font-semibold" title="@Title">@Title</h3>
        @if (!string.IsNullOrWhiteSpace(Year))
        {
            <p class="text-xs text-muted">@Year</p>
        }
    </div>
</div>

@code {
    [Parameter, EditorRequired] public string Title { get; set; } = "";
    [Parameter] public string? PosterUrl { get; set; }
    [Parameter] public string? Overview { get; set; }
    [Parameter] public string? Year { get; set; }
    [Parameter] public double Score { get; set; }
    [Parameter, EditorRequired] public string TrailerUrl { get; set; } = "";
    [Parameter, EditorRequired] public string WatchUrl { get; set; } = "";
}
